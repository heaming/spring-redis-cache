spring.application.name: spring-redis-cache

spring:
  datasource:
    url: "jdbc:mysql://localhost:3307/fastsns"
    username: root
    password: user
  output:
    ansi:
      enabled: always

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


